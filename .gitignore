# OS
.DS_Store

# direnv local cache (not the .envrc itself, which is committed)
.direnv/

# CI build dirs
staging/
release/

# MoonScript compile output (source of truth is *.moon)
/main.lua
/conf.lua
/src/**/*.lua
/tests/**/*.lua
!patterns/*.lua
