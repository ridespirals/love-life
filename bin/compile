#!/bin/sh
# Compile MoonScript sources to Lua (generated .lua is gitignored).
set -e
ROOT="$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
export PATH="${ROOT}/.luarocks/bin:${HOME}/.luarocks/bin:${PATH}"
moonc main.moon conf.moon
moonc src
moonc tests
